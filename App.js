import React from 'react';
import {StyleSheet, Text, View} from 'react-native';
import RootTabs from './app/route/route';

const App = () => {
  return RootTabs();
};

const styles = StyleSheet.create({});

export default App;
